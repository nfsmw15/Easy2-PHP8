# Kein Directory-Listing
Options -Indexes -ExecCGI

# PHP-Dateien komplett verweigern (statt php_flag engine off,
# das auf restriktiven AllowOverride-Configs einen 500 auslösen kann)
<FilesMatch "\.(php|php\d|phtml|phar)$">
    Require all denied
</FilesMatch>
